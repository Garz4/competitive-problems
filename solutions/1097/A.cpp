#include <bits/stdc++.h>

using namespace std;

int main(void) {
  string tableCard, hand;
  cin >> tableCard >> hand;
  if(hand[0] == tableCard[0] || hand[1] == tableCard[1]) {
    cout << "YES" << endl;
    return 0;
  }
  cin  >> hand;
  if(hand[0] == tableCard[0] || hand[1] == tableCard[1]) {
    cout << "YES" << endl;
    return 0;
  }
  cin  >> hand;
  if(hand[0] == tableCard[0] || hand[1] == tableCard[1]) {
    cout << "YES" << endl;
    return 0;
  }
  cin  >> hand;
  if(hand[0] == tableCard[0] || hand[1] == tableCard[1]) {
    cout << "YES" << endl;
    return 0;
  }
  cin  >> hand;
  if(hand[0] == tableCard[0] || hand[1] == tableCard[1]) {
    cout << "YES" << endl;
    return 0;
  }
  cout << "NO" << endl;
  return 0;
}
